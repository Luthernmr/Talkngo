<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        <link rel="stylesheet" href="https://bootswatch.com/4/sandstone/bootstrap.min.css">
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
      
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}
        <link rel="stylesheet" href="{{asset('css/stylePublication.css')}}">
        <link rel="stylesheet" href="{{asset('css/style.css')}}">
        <link rel="stylesheet" href="{{asset('css/styleProfil.css')}}">
        <link rel="stylesheet" href="{{asset('css/style_home.css')}}">
        <link rel="stylesheet" href="{{asset('css/form.css')}}">
        {% block javascripts %}
        <script src="{{ asset('js/searchbar.js') }}"></script>
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
        <header class="large-screen">
            
            <a href="{{path('home')}}"><img class="logo" src="{{asset('css/img/logo-horizontal-1.svg')}}" alt="logo talkngo"></a>
            <div class="header-content">
                <div class="liste-searchbar">
                <input class="searchbar" onClick="afficheListe()" type="search" name="myInput" placeholder="Chercher un pays" id="myInput" onkeydown="searchFunction(); afficheListekeyboard()">
                    <ul id="myUL">
                       {% for country in countrys %}
                       <li class="container-fluid" id="suggestion">
                            <a class="pays" id="pays" href="{{ path('country_page',{'id':country.id } ) }}">{{ country.countryName }} </a>
                        </li>
                        {% endfor %} 
                    </ul>
            </div>
            <li>
                <div class="spacebar"></div>
            </li>
    {% if app.user %}

    <li>
        <a class="register" href="{{ path('profil') }}">Mon profil</a>
        <a class="login" href="{{ path('app_logout') }}">déconnexion</a>
    </li>
    {% else %}
    <li><a class="register" href="{{ path('app_register') }}">Inscription</a></li>
    <li><a class="login" href=" {{ path('app_login') }} ">Connexion</a></li>

    {% endif %}
                
            
                
            </div>
        </header>


        <header class="tablet-screen">
            
            <a href="{{path('home')}}"><img class="logo" src="{{asset('css/img/logo-horizontal-1.svg')}}" alt="logo talkngo"></a>
            <div class="header-content">
                <div class="liste-searchbar">
                <input class="searchbar" onClick="afficheListe()" type="search" name="myInput" placeholder="Chercher un pays" id="myInput" onkeydown="searchFunction(); afficheListekeyboard()">
                    <ul id="myUL">
                       {% for country in countrys %}
                        <li>
                            <a class="pays" id="pays" href="{{ path('country_page',{'id':country.id } ) }}">{{ country.countryName }}</a>
                        </li>
                        {% endfor %} 
                    </ul>
            </div>
            <li>
                <div class="spacebar"></div>
            </li>
    {% if app.user %}

    <li>
        <a class="register" href="{{ path('profil') }}">Mon profil</a>
    </li>
    {% else %}
  
    
    <li><a class="register" href="{{ path('app_register') }}">Inscription</a></li>
    <li><a class="login" href=" {{ path('app_login') }} ">Connexion</a></li>


    {% endif %}
                
            
                
            </div>
        </header>

        <header class="small-screen">
            
            <a href="{{path('home')}}"><img class="logo" src="{{asset('css/img/logo-horizontal-1.svg')}}" alt="logo talkngo"></a>
            <div class="header-content">
                <div class="liste-searchbar">
                <input class="searchbar" onClick="afficheListe()" type="search" name="myInput" placeholder="Chercher un pays" id="myInput" onkeydown="searchFunction(); afficheListekeyboard()">
                    <ul id="myUL">
                       {% for country in countrys %}
                        <li>
                            <a class="pays" id="pays" href="{{ path('country_page',{'id':country.id } ) }}">{{ country.countryName }}</a>
                        </li>
                        {% endfor %} 
                    </ul>
            </div>
            <li>
                <div class="spacebar"></div>
            </li>
    {% if app.user %}

    <li>
        <a class="register" href="{{ path('profil') }}">Mon profil</a>
    </li>
    {% else %}
    <li><a class="register" href="{{ path('app_register') }}">Inscription</a></li>
    <li><a class="login" href=" {{ path('app_login') }} ">Connexion</a></li>

    {% endif %}
                
            
                
            </div>
        </header>
    
        <header class="phone-screen">
            <a href="{{path('home')}}"><img class="logo" src="{{asset('css/img/logo-vertical-2.svg')}}" alt="logo talkngo"></a>
            <input class="searchbar" type="search" name="" placeholder="Chercher un pays">
    
            
            <a class="register" href="{{path('app_register')}}">Inscription</a>
            <a class="login" href="">Connexion</a>
               
    
        </header>
    
        {% block body %}<body>

            <a href="{{ path('admin_user') }}">users</a>   
                   
        </body>{% endblock %}
        <footer>
            <li><a href="#">A propos</a></li>
            <li><a href="#">Nous contacter</a></li>
            <li><a href="#">Talk'n Go © Copyright 2021</a></li>
        </footer>  
        <script src="https://kit.fontawesome.com/ccb1e14628.js" crossorigin="anonymous"></script>
    </body>
</html>
